
export const getAllAds = ({ ads }) => ads;

const adsReducer = (statePart = [], action) => {
    switch(action.type) {
      default:
        return statePart
        
    };
};

export default adsReducer;  
